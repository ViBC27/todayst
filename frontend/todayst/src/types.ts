export type TaskType = { id: number; task: string; time: string; status: boolean };
export type TaskInputType = { task?: string; time?: string; status?: boolean };
export type DoneType = { done: boolean };
